/* ═══════════════════════════════════════════════════════
   AURUM — COMPLETE ENGINE + DUAL THEME
   ═══════════════════════════════════════════════════════ */
const WA='91XXXXXXXXXX';
const WA_ICO=sz=>`<svg width="${sz}" height="${sz}" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>`;
const PRODUCTS=[
{id:1,name:"Celestial Gold Ring",category:"Rings",price:25000,image:"https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=600&h=720&fit=crop",desc:"An exquisite 22K gold ring with celestial-inspired design. Handcrafted by master artisans."},
{id:2,name:"Royal Diamond Necklace",category:"Necklaces",price:85000,image:"https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?w=600&h=720&fit=crop",desc:"Stunning diamond-studded necklace in 18K white gold with brilliant-cut diamonds."},
{id:3,name:"Pearl Drop Earrings",category:"Earrings",price:18000,image:"https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?w=600&h=720&fit=crop",desc:"Elegant freshwater pearl drop earrings with 14K gold hooks."},
{id:4,name:"Sapphire Eternity Ring",category:"Rings",price:42000,image:"https://images.unsplash.com/photo-1603561591411-07134e71a2a9?w=600&h=720&fit=crop",desc:"Breathtaking eternity ring with channel-set blue sapphires in platinum."},
{id:5,name:"Emerald Pendant",category:"Necklaces",price:67000,image:"https://images.unsplash.com/photo-1611591437281-460bfbe1220a?w=600&h=720&fit=crop",desc:"Magnificent 2-carat Colombian emerald pendant on 18K gold chain."},
{id:6,name:"Diamond Hoop Earrings",category:"Earrings",price:35000,image:"https://images.unsplash.com/photo-1630019852942-f89202989a59?w=600&h=720&fit=crop",desc:"48 brilliant-cut diamonds in 18K rose gold hoops."},
{id:7,name:"Vintage Rose Ring",category:"Rings",price:31000,image:"https://images.unsplash.com/photo-1602751584552-8ba73aad10e1?w=600&h=720&fit=crop",desc:"Vintage-inspired rose gold ring with filigree and morganite."},
{id:8,name:"Layered Gold Necklace",category:"Necklaces",price:28000,image:"https://images.unsplash.com/photo-1515562141589-67f0d569b6f5?w=600&h=720&fit=crop",desc:"Three delicate 22K gold chains with unique pendant charms."},
{id:9,name:"Ruby Stud Earrings",category:"Earrings",price:22000,image:"https://images.unsplash.com/photo-1588444837495-c6cfeb53f32d?w=600&h=720&fit=crop",desc:"Perfectly matched Burmese ruby studs in 18K yellow gold."},
{id:10,name:"Platinum Tennis Bracelet",category:"Bracelets",price:95000,image:"https://images.unsplash.com/photo-1573408301185-9146fe634ad0?w=600&h=720&fit=crop",desc:"36 round brilliant-cut diamonds totaling 5 carats in platinum."},
{id:11,name:"Gold Charm Bracelet",category:"Bracelets",price:19000,image:"https://images.unsplash.com/photo-1611652022419-a9419f74343d?w=600&h=720&fit=crop",desc:"Playful 22K gold bracelet with heart, star, moon, and key charms."},
{id:12,name:"Art Deco Diamond Ring",category:"Rings",price:56000,image:"https://images.unsplash.com/photo-1598560917505-59a3ad559071?w=600&h=720&fit=crop",desc:"Art Deco platinum ring with geometric patterns and princess-cut diamond."}
];

const DB={get(k){try{return JSON.parse(localStorage.getItem('aurum_'+k))}catch{return null}},set(k,v){localStorage.setItem('aurum_'+k,JSON.stringify(v))},del(k){localStorage.removeItem('aurum_'+k)}};
let currentUser=DB.get('user')||null,cart=DB.get('cart')||[],wishlist=DB.get('wish')||[],addresses=DB.get('addr')||[],users=DB.get('users')||[];
function saveState(){DB.set('cart',cart);DB.set('wish',wishlist);DB.set('addr',addresses);DB.set('users',users);if(currentUser)DB.set('user',currentUser)}
const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s),fp=p=>'₹'+Number(p).toLocaleString('en-IN'),waLink=(n,p)=>`https://wa.me/${WA}?text=${encodeURIComponent(`Hello! I want to order:\nProduct: ${n}\nPrice: ${fp(p)}\nPlease share details.`)}`;

/* ═══ THEME ENGINE ═══ */
function getTheme(){return DB.get('theme')||'dark'}
function setTheme(t){
  const flash=$('#theme-flash');flash.classList.add('active');setTimeout(()=>flash.classList.remove('active'),300);
  document.documentElement.classList.add('theme-transitioning');
  document.documentElement.setAttribute('data-theme',t);
  DB.set('theme',t);
  $('#tl-dark').classList.toggle('active',t==='dark');
  $('#tl-light').classList.toggle('active',t==='light');
  setTimeout(()=>document.documentElement.classList.remove('theme-transitioning'),700);
}
function initTheme(){const t=getTheme();document.documentElement.setAttribute('data-theme',t);$('#tl-dark').classList.toggle('active',t==='dark');$('#tl-light').classList.toggle('active',t==='light')}
$('#theme-switch').addEventListener('click',()=>{const cur=document.documentElement.getAttribute('data-theme');setTheme(cur==='dark'?'light':'dark')});
initTheme();

/* Toast */
function toast(msg,type='info'){const w=$('#toast-wrap'),t=document.createElement('div');t.className='toast';t.innerHTML=`<span class="toast-icon ${type}">${type==='success'?'✓':type==='error'?'✕':'✦'}</span><span>${msg}</span>`;w.appendChild(t);setTimeout(()=>{t.classList.add('out');setTimeout(()=>t.remove(),400)},3e3)}

/* Panels */
function openPanel(id){$('#'+id).classList.add('open');document.body.style.overflow='hidden'}
function closePanel(id){$('#'+id).classList.remove('open');document.body.style.overflow=''}
document.addEventListener('click',e=>{const c=e.target.closest('[data-close]');if(c)closePanel(c.dataset.close)});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){$$('.panel-overlay.open').forEach(p=>p.classList.remove('open'));document.body.style.overflow='';$('#det-ov').classList.remove('open')}});

/* Auth */
function showAuthTab(tab){$$('.auth-tab').forEach(t=>t.classList.toggle('active',t.dataset.authTab===tab));$$('.auth-form').forEach(f=>f.classList.remove('active'));$('#auth-'+tab).classList.add('active')}
$$('.auth-tab').forEach(t=>t.addEventListener('click',()=>showAuthTab(t.dataset.authTab)));
function togglePwd(id,btn){const i=$('#'+id);i.type=i.type==='password'?'text':'password';btn.textContent=i.type==='password'?'Show':'Hide'}
function vf(id,test){$('#'+id).closest('.form-group').classList.toggle('error',!test);return test}

$('#login-btn').addEventListener('click',()=>{const email=$('#login-email').value.trim(),pwd=$('#login-pwd').value;let ok=true;ok=vf('login-email',/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))&&ok;ok=vf('login-pwd',pwd.length>0)&&ok;if(!ok)return;const user=users.find(u=>u.email===email&&u.pwd===pwd);if(!user){toast('Invalid credentials','error');return}currentUser=user;addresses=user.addresses||[];DB.set('user',currentUser);DB.set('addr',addresses);closePanel('auth-panel');updateNav();toast('Welcome back, '+user.fname+'!','success')});
$('#reg-btn').addEventListener('click',()=>{const fn=$('#reg-fname').value.trim(),ln=$('#reg-lname').value.trim(),em=$('#reg-email').value.trim(),ph=$('#reg-phone').value.trim(),pw=$('#reg-pwd').value;let ok=true;ok=vf('reg-fname',fn.length>0)&&ok;ok=vf('reg-lname',ln.length>0)&&ok;ok=vf('reg-email',/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(em))&&ok;ok=vf('reg-phone',ph.length>=10)&&ok;ok=vf('reg-pwd',pw.length>=6)&&ok;if(!ok)return;if(users.find(u=>u.email===em)){toast('Account exists','error');return}const user={id:Date.now(),fname:fn,lname:ln,email:em,phone:ph,pwd:pw,addresses:[]};users.push(user);currentUser=user;addresses=[];saveState();closePanel('auth-panel');updateNav();toast('Welcome, '+fn+'!','success')});
$('#logout-btn').addEventListener('click',()=>{currentUser=null;cart=[];wishlist=[];addresses=[];DB.del('user');DB.set('cart',[]);DB.set('wish',[]);DB.set('addr',[]);closePanel('account-panel');updateNav();updateBadges();renderGrid();toast('Signed out','info')});
$('#nav-user').addEventListener('click',()=>{if(!currentUser){openPanel('auth-panel');return}renderAccountPanel();openPanel('account-panel')});
function renderAccountPanel(){$('#account-body').innerHTML=`<div style="margin-bottom:24px"><div style="font-family:var(--fd);font-size:1.2rem;margin-bottom:3px">${currentUser.fname} ${currentUser.lname}</div><div style="font-size:.78rem;color:var(--text-secondary)">${currentUser.email}</div><div style="font-size:.75rem;color:var(--text-muted);margin-top:2px">${currentUser.phone||''}</div></div><div style="font-size:.62rem;letter-spacing:.14em;text-transform:uppercase;color:var(--accent);margin-bottom:14px;display:flex;justify-content:space-between;align-items:center">Saved Addresses <button class="btn-outline-sm" onclick="editAddress(-1)">+ Add</button></div><div>${renderAddrList()}</div>`}
function renderAddrList(){if(!addresses.length)return'<div style="text-align:center;padding:20px;color:var(--text-muted);font-size:.82rem">No saved addresses</div>';return addresses.map((a,i)=>`<div class="addr-card"><div class="addr-card-label">${a.type} ${a.isDefault?'<span class="tag">Default</span>':''}</div><div class="addr-card-name">${a.name}</div><div class="addr-card-text">${a.line1}${a.line2?', '+a.line2:''}<br>${a.city}, ${a.state} - ${a.pin}</div><div class="addr-card-phone">${a.phone}</div><div class="addr-card-actions"><button class="link-btn" onclick="editAddress(${i})">Edit</button><button class="link-btn" style="color:var(--red)" onclick="deleteAddress(${i})">Delete</button>${!a.isDefault?`<button class="link-btn" onclick="setDefAddr(${i})">Set Default</button>`:''}</div></div>`).join('')}

let editIdx=-1;
function editAddress(i){editIdx=i;const a=i>=0?addresses[i]:{};$('#addr-panel-title').textContent=i>=0?'Edit Address':'Add Address';$('#addr-name').value=a.name||'';$('#addr-phone').value=a.phone||'';$('#addr-line1').value=a.line1||'';$('#addr-line2').value=a.line2||'';$('#addr-city').value=a.city||'';$('#addr-state').value=a.state||'';$('#addr-pin').value=a.pin||'';$('#addr-type').value=a.type||'Home';openPanel('addr-panel')}
$('#addr-save-btn').addEventListener('click',()=>{const n=$('#addr-name').value.trim(),p=$('#addr-phone').value.trim(),l1=$('#addr-line1').value.trim(),l2=$('#addr-line2').value.trim(),c=$('#addr-city').value.trim(),s=$('#addr-state').value.trim(),pin=$('#addr-pin').value.trim(),t=$('#addr-type').value;let ok=true;ok=vf('addr-name',n.length>0)&&ok;ok=vf('addr-phone',p.length>=10)&&ok;ok=vf('addr-line1',l1.length>0)&&ok;ok=vf('addr-city',c.length>0)&&ok;ok=vf('addr-state',s.length>0)&&ok;ok=vf('addr-pin',pin.length>=5)&&ok;if(!ok)return;const addr={name:n,phone:p,line1:l1,line2:l2,city:c,state:s,pin:pin,isDefault:!addresses.length};if(editIdx>=0)Object.assign(addresses[editIdx],addr);else addresses.push(addr);if(currentUser){currentUser.addresses=addresses;const ui=users.findIndex(u=>u.id===currentUser.id);if(ui>=0)users[ui]=currentUser}saveState();closePanel('addr-panel');if($('#account-panel').classList.contains('open'))renderAccountPanel();if($('#checkout-panel').classList.contains('open'))renderCheckout();toast('Address saved','success')});
function deleteAddress(i){addresses.splice(i,1);if(addresses.length&&!addresses.find(a=>a.isDefault))addresses[0].isDefault=true;if(currentUser){currentUser.addresses=addresses;const ui=users.findIndex(u=>u.id===currentUser.id);if(ui>=0)users[ui]=currentUser}saveState();renderAccountPanel();toast('Removed','info')}
function setDefAddr(i){addresses.forEach((a,j)=>a.isDefault=j===i);if(currentUser){currentUser.addresses=addresses;const ui=users.findIndex(u=>u.id===currentUser.id);if(ui>=0)users[ui]=currentUser}saveState();renderAccountPanel();toast('Default updated','success')}

/* Cart */
function addToCart(id){if(!currentUser){toast('Please sign in','error');openPanel('auth-panel');return}const e=cart.find(c=>c.id===id);if(e){e.qty++;toast('Qty updated','success')}else{cart.push({id,qty:1});toast('Added to bag','success')}saveState();updateBadges();renderGrid()}
function removeFromCart(id){cart=cart.filter(c=>c.id!==id);saveState();updateBadges();renderCartPanel()}
function updateQty(id,d){const c=cart.find(x=>x.id===id);if(!c)return;c.qty=Math.max(1,c.qty+d);saveState();renderCartPanel()}
function getTotal(){return cart.reduce((s,c)=>{const p=PRODUCTS.find(x=>x.id===c.id);return s+(p?p.price*c.qty:0)},0)}
function renderCartPanel(){const b=$('#cart-body'),f=$('#cart-footer');if(!cart.length){b.innerHTML='<div class="cart-empty"><div class="cart-empty-icon">🛍</div><div class="cart-empty-text">Your bag is empty</div><button class="btn btn-ghost btn-sm" onclick="closePanel(\'cart-panel\')">Continue Shopping</button></div>';f.innerHTML='';return}b.innerHTML=cart.map(c=>{const p=PRODUCTS.find(x=>x.id===c.id);if(!p)return'';return`<div class="cart-item"><div class="cart-item-img"><img src="${p.image}"></div><div class="cart-item-info"><div class="cart-item-name">${p.name}</div><div class="cart-item-cat">${p.category}</div><div class="cart-item-price">${fp(p.price*c.qty)}</div><div class="cart-item-actions"><div class="qty-wrap"><button class="qty-btn" onclick="updateQty(${p.id},-1)">−</button><div class="qty-val">${c.qty}</div><button class="qty-btn" onclick="updateQty(${p.id},1)">+</button></div><button class="cart-item-remove" onclick="removeFromCart(${p.id})">Remove</button></div></div></div>`}).join('');const t=getTotal();f.innerHTML=`<div style="display:flex;flex-direction:column;gap:8px"><div class="cart-row"><span>Subtotal</span><span>${fp(t)}</span></div><div class="cart-row"><span>Shipping</span><span style="color:var(--green)">Free</span></div><div class="cart-row total"><span>Total</span><span>${fp(t)}</span></div></div><div class="cart-btns"><button class="btn btn-gold" onclick="startCheckout()">Checkout</button><a href="${waCart()}" target="_blank" class="btn btn-wa">${WA_ICO(13)} WhatsApp Order</a></div>`}
function waCart(){const items=cart.map(c=>{const p=PRODUCTS.find(x=>x.id===c.id);return p?`• ${p.name} x${c.qty} — ${fp(p.price*c.qty)}`:''}).join('\n');return`https://wa.me/${WA}?text=${encodeURIComponent(`Hello! I'd like to order:\n\n${items}\n\nTotal: ${fp(getTotal())}`)}`}
$('#nav-cart').addEventListener('click',()=>{renderCartPanel();openPanel('cart-panel')});

/* Wishlist */
function toggleWish(id){if(!currentUser){toast('Please sign in','error');openPanel('auth-panel');return}const i=wishlist.indexOf(id);if(i>=0){wishlist.splice(i,1);toast('Removed from wishlist','info')}else{wishlist.push(id);toast('Added to wishlist','success')}saveState();updateBadges();renderGrid()}
function renderWishPanel(){const b=$('#wish-body');if(!wishlist.length){b.innerHTML='<div class="cart-empty"><div class="cart-empty-icon">♡</div><div class="cart-empty-text">Wishlist is empty</div></div>';return}b.innerHTML=wishlist.map(id=>{const p=PRODUCTS.find(x=>x.id===id);if(!p)return'';return`<div class="wish-item"><div class="wish-item-img"><img src="${p.image}"></div><div class="wish-item-info"><div class="wish-item-name">${p.name}</div><div class="wish-item-price">${fp(p.price)}</div><div class="wish-item-actions"><button class="btn-outline-sm" onclick="addToCart(${p.id});toggleWish(${p.id});renderWishPanel()">Add to Bag</button><button class="btn-outline-sm" style="border-color:rgba(231,76,60,.2);color:var(--red)" onclick="toggleWish(${p.id});renderWishPanel()">Remove</button></div></div></div>`}).join('')}
$('#nav-wish').addEventListener('click',()=>{renderWishPanel();openPanel('wish-panel')});

/* Checkout */
let ckStep=0,selAddr=-1,selPay='whatsapp';
function startCheckout(){if(!currentUser){toast('Sign in first','error');closePanel('cart-panel');openPanel('auth-panel');return}if(!cart.length){toast('Cart empty','error');return}ckStep=0;selAddr=addresses.findIndex(a=>a.isDefault);if(selAddr<0&&addresses.length)selAddr=0;selPay='whatsapp';closePanel('cart-panel');renderCheckout();openPanel('checkout-panel')}
function renderCheckout(){const b=$('#checkout-body'),steps=['Address','Payment','Confirm'];b.innerHTML=`<div class="checkout-steps">${steps.map((s,i)=>`<div class="checkout-step ${i===ckStep?'active':i<ckStep?'done':''}">${i<ckStep?'✓':i+1}. ${s}</div>`).join('')}</div><div class="checkout-view ${ckStep===0?'active':''}" id="ck0"></div><div class="checkout-view ${ckStep===1?'active':''}" id="ck1"></div><div class="checkout-view ${ckStep===2?'active':''}" id="ck2"></div>`;
if(ckStep===0){$('#ck0').innerHTML=`<div style="font-size:.68rem;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);margin-bottom:14px;display:flex;justify-content:space-between;align-items:center">Delivery Address <button class="btn-outline-sm" onclick="editAddress(-1)">+ Add</button></div>${!addresses.length?'<div style="text-align:center;padding:28px;color:var(--text-muted)">Add address to continue</div>':addresses.map((a,i)=>`<div class="addr-card ${i===selAddr?'selected':''}" onclick="selAddr=${i};renderCheckout()" style="cursor:pointer"><div class="addr-check">✓</div><div class="addr-card-label">${a.type}</div><div class="addr-card-name">${a.name}</div><div class="addr-card-text">${a.line1}${a.line2?', '+a.line2:''}, ${a.city}, ${a.state} - ${a.pin}</div><div class="addr-card-phone">${a.phone}</div></div>`).join('')}${addresses.length&&selAddr>=0?`<button class="btn btn-gold" style="margin-top:14px" onclick="ckStep=1;renderCheckout()">Continue</button>`:''}`}
if(ckStep===1){$('#ck1').innerHTML=`<div style="font-size:.68rem;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);margin-bottom:14px">Payment Method</div>${[{id:'whatsapp',l:'WhatsApp Order',d:'Place via WhatsApp'},{id:'cod',l:'Cash on Delivery',d:'Pay on delivery'},{id:'upi',l:'UPI Payment',d:'GPay, PhonePe, Paytm'},{id:'card',l:'Card Payment',d:'Credit / Debit Card'}].map(p=>`<div class="pay-option ${selPay===p.id?'selected':''}" onclick="selPay='${p.id}';renderCheckout()"><div class="pay-radio"></div><div><div class="pay-option-label">${p.l}</div><div class="pay-option-desc">${p.d}</div></div></div>`).join('')}<div style="display:flex;gap:9px;margin-top:18px"><button class="btn btn-ghost btn-sm" onclick="ckStep=0;renderCheckout()">Back</button><button class="btn btn-gold" onclick="ckStep=2;renderCheckout()" style="flex:1">Review</button></div>`}
if(ckStep===2){const a=addresses[selAddr],t=getTotal();$('#ck2').innerHTML=`<div style="font-size:.68rem;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);margin-bottom:10px">Order Summary</div>${cart.map(c=>{const p=PRODUCTS.find(x=>x.id===c.id);if(!p)return'';return`<div class="order-mini-item"><div class="order-mini-img"><img src="${p.image}"></div><div><div class="order-mini-name">${p.name}</div><div class="order-mini-qty">Qty: ${c.qty}</div></div><div class="order-mini-price">${fp(p.price*c.qty)}</div></div>`}).join('')}<div style="margin:16px 0;padding:14px 0;border-top:1px solid var(--border);border-bottom:1px solid var(--border)"><div class="cart-row"><span>Subtotal</span><span>${fp(t)}</span></div><div class="cart-row"><span>Shipping</span><span style="color:var(--green)">Free</span></div><div class="cart-row total"><span>Total</span><span>${fp(t)}</span></div></div><div style="font-size:.68rem;letter-spacing:.12em;text-transform:uppercase;color:var(--accent);margin-bottom:6px">Delivery</div><div style="font-size:.8rem;color:var(--text-secondary);line-height:1.7;margin-bottom:14px">${a.name}<br>${a.line1}${a.line2?', '+a.line2:''}<br>${a.city}, ${a.state} - ${a.pin}</div><div style="display:flex;gap:9px"><button class="btn btn-ghost btn-sm" onclick="ckStep=1;renderCheckout()">Back</button><button class="btn btn-gold" onclick="placeOrder()">Place Order</button></div>`}}
function placeOrder(){const oid='AU-'+Date.now().toString(36).toUpperCase(),t=getTotal(),a=addresses[selAddr];$('#checkout-body').innerHTML=`<div class="order-success"><div class="order-success-icon">✓</div><h3>Order Placed!</h3><div class="order-id">${oid}</div><p>Thank you, ${currentUser.fname}!</p><p style="color:var(--text-secondary)">Total: <strong style="color:var(--accent)">${fp(t)}</strong></p><p style="font-size:.76rem;margin-top:6px">Delivering to: ${a.city}, ${a.state}</p><div style="display:flex;flex-direction:column;gap:9px;margin-top:24px">${selPay==='whatsapp'?`<a href="${waCart()}" target="_blank" class="btn btn-wa">${WA_ICO(13)} Confirm on WhatsApp</a>`:''}<button class="btn btn-ghost" onclick="closePanel('checkout-panel')">Continue Shopping</button></div></div>`;cart=[];saveState();updateBadges();renderGrid();toast('Order placed!','success')}

/* Product Detail */
function openDetail(id){const p=PRODUCTS.find(x=>x.id===id);if(!p)return;const ic=cart.find(c=>c.id===id),iw=wishlist.includes(id);$('#det-p').innerHTML=`<button class="det-cls" onclick="closeDet()" data-h>✕</button><div class="det-img"><img src="${p.image}"></div><div class="det-info"><div class="det-bc">Home / ${p.category} / <span>${p.name}</span></div><h2 class="det-nm">${p.name}</h2><div class="det-pr">${fp(p.price)}</div><div class="det-div"></div><p class="det-desc">${p.desc}</p><div class="det-mr"><span class="det-ml">Category</span><span class="det-mv">${p.category}</span></div><div class="det-mr"><span class="det-ml">ID</span><span class="det-mv">#AU-${String(p.id).padStart(4,'0')}</span></div><div class="det-mr"><span class="det-ml">Status</span><span class="det-mv" style="color:var(--green)">In Stock</span></div><div class="det-btns"><button class="btn btn-gold" onclick="addToCart(${p.id});closeDet()">${ic?'✓ In Bag':'Add to Bag'}</button><a href="${waLink(p.name,p.price)}" target="_blank" class="btn btn-wa">${WA_ICO(13)} WhatsApp</a></div><div style="margin-top:10px;display:flex;gap:10px"><button class="btn-outline-sm" onclick="toggleWish(${p.id});closeDet()" style="flex:1">${iw?'♥ Wishlisted':'♡ Wishlist'}</button><button class="btn-outline-sm" onclick="buyNow(${p.id})" style="flex:1">Buy Now</button></div></div>`;$('#det-ov').classList.add('open');document.body.style.overflow='hidden';initCH()}
function closeDet(){$('#det-ov').classList.remove('open');document.body.style.overflow=''}
$('#det-bg').addEventListener('click',closeDet);
function buyNow(id){if(!currentUser){toast('Sign in first','error');closeDet();openPanel('auth-panel');return}if(!cart.find(c=>c.id===id))cart.push({id,qty:1});saveState();updateBadges();closeDet();startCheckout()}

/* Nav/Badges */
function updateNav(){$('#nav-uname').textContent=currentUser?currentUser.fname:''}
function updateBadges(){const cc=cart.reduce((s,c)=>s+c.qty,0);$('#cart-badge').textContent=cc;$('#cart-badge').classList.toggle('show',cc>0);$('#wish-badge').textContent=wishlist.length;$('#wish-badge').classList.toggle('show',wishlist.length>0)}

/* Grid */
let activeFilter='All';
function renderGrid(){const g=$('#prod-grid'),fl=activeFilter==='All'?PRODUCTS:PRODUCTS.filter(p=>p.category===activeFilter);g.style.opacity='0';g.style.transform='translateY(10px)';setTimeout(()=>{g.innerHTML=fl.map((p,i)=>{const ic=cart.find(c=>c.id===p.id),iw=wishlist.includes(p.id);return`<div class="pc ru s${(i%8)+1}"><div class="pc-img"><img src="${p.image}" loading="lazy"><div class="pc-ov"><button class="pc-qb gold" onclick="event.stopPropagation();openDetail(${p.id})" data-h>View</button><button class="pc-qb gh" onclick="event.stopPropagation();addToCart(${p.id})" data-h>${ic?'✓ Added':'Add'}</button></div><button class="pc-wish ${iw?'liked':''}" onclick="event.stopPropagation();toggleWish(${p.id})" data-h>${iw?'♥':'♡'}</button></div><div class="pc-info" onclick="openDetail(${p.id})" style="cursor:pointer"><div class="pc-cat">${p.category}</div><span class="pc-name" data-h>${p.name}</span><div class="pc-bot"><div class="pc-price">${fp(p.price)}</div><button class="pc-cart-btn ${ic?'in-cart':''}" onclick="event.stopPropagation();addToCart(${p.id})" data-h>+</button></div></div></div>`}).join('');g.style.transition='opacity .5s,transform .5s';g.style.opacity='1';g.style.transform='translateY(0)';initReveals();initCH()},220)}

/* Filters */
(()=>{const cats=['All',...new Set(PRODUCTS.map(p=>p.category))],bar=$('#filter-bar');bar.innerHTML=cats.map(c=>`<button class="fbtn${c==='All'?' act':''}" data-cat="${c}" data-h>${c}</button>`).join('');bar.addEventListener('click',e=>{const b=e.target.closest('.fbtn');if(!b)return;activeFilter=b.dataset.cat;$$('.fbtn').forEach(x=>x.classList.toggle('act',x.dataset.cat===activeFilter));renderGrid()})})();

/* Categories */
(()=>{const cats=[{n:'Rings',img:'https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=500&h=400&fit=crop'},{n:'Necklaces',img:'https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?w=500&h=400&fit=crop'},{n:'Earrings',img:'https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?w=500&h=400&fit=crop'},{n:'Bracelets',img:'https://images.unsplash.com/photo-1573408301185-9146fe634ad0?w=500&h=400&fit=crop'}],g=$('#cat-grid');g.innerHTML=cats.map((c,i)=>{const cnt=PRODUCTS.filter(p=>p.category===c.n).length;return`<a href="#products" class="ru s${i+1}" onclick="event.preventDefault();activeFilter='${c.n}';$$('.fbtn').forEach(x=>x.classList.toggle('act',x.dataset.cat==='${c.n}'));renderGrid();document.getElementById('products').scrollIntoView({behavior:'smooth'})" style="position:relative;height:340px;overflow:hidden;display:block"><img src="${c.img}" loading="lazy" style="width:100%;height:100%;object-fit:cover;filter:brightness(.4) saturate(.7);transition:all 1s var(--ease)"><div style="position:absolute;inset:0;display:flex;flex-direction:column;justify-content:flex-end;padding:28px;z-index:2;background:linear-gradient(to top,rgba(10,10,10,.8),transparent 60%)"><div style="font-family:var(--fd);font-size:1.3rem;color:#fff">${c.n}</div><div style="font-size:.62rem;letter-spacing:.12em;text-transform:uppercase;color:rgba(255,255,255,.6);margin-top:3px">${cnt} Pieces</div></div></a>`}).join('')})();

/* Marquee */
(()=>{const items=['Handcrafted with Love','22K & 18K Gold','Certified Diamonds','Free Shipping India','Lifetime Exchange','BIS Hallmarked','Master Artisans'],h=items.map(t=>`<span class="marq-i">${t} <span class="marq-di">✦</span></span>`).join('');$('#marq-t').innerHTML=h+h})();

/* Init */
window.addEventListener('load',()=>{setTimeout(()=>$('#loader').classList.add('done'),900)});
window.addEventListener('scroll',()=>$('#nav').classList.toggle('sc',scrollY>50));
$('#nav-toggle').addEventListener('click',()=>{$('#nav-toggle').classList.toggle('open');$('#nav-links').classList.toggle('open');document.body.style.overflow=$('#nav-links').classList.contains('open')?'hidden':''});
$$('#nav-links a').forEach(a=>a.addEventListener('click',()=>{$('#nav-toggle').classList.remove('open');$('#nav-links').classList.remove('open');document.body.style.overflow=''}));
$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id==='#')return;const t=$(id);if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));

function initReveals(){const obs=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting){x.target.classList.add('vis');obs.unobserve(x.target)}})},{threshold:.06,rootMargin:'0px 0px -20px 0px'});$$('.ru:not(.vis)').forEach(el=>obs.observe(el))}initReveals();

(()=>{const obs=new IntersectionObserver(e=>{e.forEach(x=>{if(!x.isIntersecting)return;const el=x.target,tgt=parseInt(el.dataset.count),dur=2e3,start=performance.now();(function tick(now){const t=Math.min((now-start)/dur,1),ease=1-Math.pow(1-t,4);el.textContent=Math.floor(ease*tgt).toLocaleString('en-IN')+'+';if(t<1)requestAnimationFrame(tick)})(performance.now());obs.unobserve(el)})},{threshold:.5});$$('[data-count]').forEach(c=>obs.observe(c))})();

/* Cursor */
function initCH(){if(innerWidth<769)return;const r=$('.cur-r');if(!r)return;$$('[data-h]').forEach(el=>{el.onmouseenter=()=>r.classList.add('h');el.onmouseleave=()=>r.classList.remove('h')})}
(()=>{const d=$('.cur-d'),r=$('.cur-r');if(!d||!r||innerWidth<769)return;let mx=0,my=0,rx=0,ry=0;document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;d.style.left=mx+'px';d.style.top=my+'px'});(function loop(){rx+=(mx-rx)*.12;ry+=(my-ry)*.12;r.style.left=rx+'px';r.style.top=ry+'px';requestAnimationFrame(loop)})();initCH()})();

/* Particles — reads CSS vars for color */
(()=>{const c=$('#ptc');if(!c)return;const ctx=c.getContext('2d');let W,H,pts=[];function rs(){W=c.width=innerWidth;H=c.height=innerHeight}rs();addEventListener('resize',rs);class P{constructor(){this.r()}r(){this.x=Math.random()*W;this.y=Math.random()*H;this.s=Math.random()*1.2+.3;this.sx=(Math.random()-.5)*.22;this.sy=(Math.random()-.5)*.22;this.o=Math.random()*.2+.04;this.p=Math.random()*Math.PI*2}u(){this.x+=this.sx;this.y+=this.sy;this.p+=.01;if(this.x<0||this.x>W||this.y<0||this.y>H)this.r()}d(){const cs=getComputedStyle(document.documentElement);const pr=cs.getPropertyValue('--particle-r').trim(),pg=cs.getPropertyValue('--particle-g').trim(),pb=cs.getPropertyValue('--particle-b').trim();ctx.fillStyle=`rgba(${pr},${pg},${pb},${this.o*(.5+Math.sin(this.p)*.5)})`;ctx.beginPath();ctx.arc(this.x,this.y,this.s,0,Math.PI*2);ctx.fill()}}for(let i=0;i<45;i++)pts.push(new P);(function a(){ctx.clearRect(0,0,W,H);pts.forEach(p=>{p.u();p.d()});requestAnimationFrame(a)})()})();

updateNav();updateBadges();renderGrid();
